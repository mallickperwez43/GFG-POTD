
class Solution {
  public:
    int rotateDelete(vector<int> &arr) {
        // Your code here
        int n = arr.size();
        int temp = n / 2;
        int i = 3 * (temp / 2);
        
        if(temp & 1) i++;
        
        return arr[n - i];
    }
};
